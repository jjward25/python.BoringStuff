# Tuples are immutable lists.  They use () instead of []
# A single value tuple can be indicated with a comma ('tuple',)
print(type(('tuple',)))

# Tuples are for when you want to indicate that this series will not change

# To convert lists to tuples and vice versa just use the name
tuple(['cat','dog','bird'])
list(('cat','god','bird'))